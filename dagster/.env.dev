# ── Dev Environment ────────────────────────────────────────────
# Development Dagster configuration.
# Usage:
#   Local:  source .env.dev && dagster dev
#   Docker: docker compose --env-file .env.dev --project-name lakehouse-dev up -d

# ── Catalog Backend ───────────────────────────────────────────
LAKEHOUSE_BACKEND=aws-glue
AWS_REGION=us-east-1
# AWS_PROFILE=default

# ── Nessie (if using aws-nessie backend) ─────────────────────
# NESSIE_URI=http://lakehouse-dev-nessie-1234567890.us-east-1.elb.amazonaws.com/api/v2

# ── GCP (if using gcp backend) ───────────────────────────────
# ICEBERG_REST_URI=
# GCP_PROJECT_ID=

# ── Access Control ────────────────────────────────────────────
# Full access in dev — no column masking
LAKEHOUSE_ACCESS_LEVEL=admin

# ── Alerting ──────────────────────────────────────────────────
# Disabled in dev
# ALERT_SNS_TOPIC_ARN=
# ALERT_SLACK_WEBHOOK_URL=
