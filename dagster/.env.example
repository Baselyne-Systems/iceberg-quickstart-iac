# ── Catalog Backend (required) ─────────────────────────────────
# Which Iceberg catalog to connect to: aws-glue, aws-nessie, or gcp
LAKEHOUSE_BACKEND=aws-glue

# ── AWS Settings ───────────────────────────────────────────────
# AWS region where your lakehouse infrastructure lives
AWS_REGION=us-east-1

# AWS CLI profile to use for credentials (default: "default")
# AWS_PROFILE=default

# ── Nessie Settings (required when LAKEHOUSE_BACKEND=aws-nessie) ──
# Nessie REST API endpoint — get this from: terraform output nessie_endpoint
# NESSIE_URI=http://lakehouse-prod-nessie-1234567890.us-east-1.elb.amazonaws.com/api/v2

# ── GCP Settings (required when LAKEHOUSE_BACKEND=gcp) ────────
# Iceberg REST catalog endpoint for GCP
# ICEBERG_REST_URI=

# GCP project ID (e.g., "my-project-123")
# GCP_PROJECT_ID=

# ── Access Control ─────────────────────────────────────────────
# Controls PII column masking in Dagster assets: admin, writer, or reader
# "reader" masks columns marked access_level=restricted in table templates
# LAKEHOUSE_ACCESS_LEVEL=admin

# ── Alerting (optional) ───────────────────────────────────────
# SNS topic ARN for schema-drift and quality-check alerts
# Get this from: terraform output alarm_sns_topic_arn (or create your own)
# ALERT_SNS_TOPIC_ARN=arn:aws:sns:us-east-1:123456789012:lakehouse-alerts

# Slack incoming webhook URL for alerts (alternative/addition to SNS)
# ALERT_SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T.../B.../xxx
